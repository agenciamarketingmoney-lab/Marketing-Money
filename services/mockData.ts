
import { Company, Campaign, DailyMetrics, User, UserRole, Task, TaskStage } from "../types";

// Arquivo limpo para produção. Dados agora vem exclusivamente do Firestore.
export const MOCK_USER: any = null;
export const MOCK_CLIENT_USER: any = null;
export const MOCK_COMPANIES: Company[] = [];
export const MOCK_CAMPAIGNS: Campaign[] = [];
export const MOCK_METRICS: DailyMetrics[] = [];
export const MOCK_TASKS: Task[] = [];
